[package]
name = "zbi"
version = "0.1.0"
edition = "2021"

[dependencies]
zerocopy = { version = "0.7.11", default-features = false, features = ["derive"] }
bitflags = "1"

[dev-dependencies]
hex = "0"
